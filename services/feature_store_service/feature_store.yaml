project: feature_repo
registry: registry.db
provider: local

online_store:
    type: redis
    host: "http:localhost"
    port: 6379
    path: "online_store.db"
    index_type: "IVF_FLAT"
    metric_type: "COSINE"
    
offline_store:
    type: file

entity_key_serialization_version: 3
auth:
    type: no_auth
